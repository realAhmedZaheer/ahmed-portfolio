# Google AI Studio (Gemini) API key - powers the live agent tool-call demo
# on /work. Get one at https://aistudio.google.com/apikey
# Without it, the demo gracefully falls back to illustrative routing.
GEMINI_API_KEY=

# Optional: override the Gemini model (default: gemini-3.1-flash-lite)
# GEMINI_MODEL=gemini-3.1-flash-lite
